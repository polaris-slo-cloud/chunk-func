---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: functiondescriptions.chunk-func.polaris-slo-cloud.github.io
spec:
  group: chunk-func.polaris-slo-cloud.github.io
  names:
    kind: FunctionDescription
    listKind: FunctionDescriptionList
    plural: functiondescriptions
    singular: functiondescription
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: FunctionDescription is the Schema for the functiondescriptions
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: FunctionDescriptionSpec defines the desired state of FunctionDescription
            properties:
              functionContainer:
                description: The name of the container in the pod that hosts the function
                  code.
                type: string
              functionName:
                description: The name of the function. This must be the name of a
                  Knative Service object in the same namespace as the FunctionDescription.
                type: string
              maxResponseTimeMs:
                description: The SLO that describes the maximum allowed response time
                  (in milliseconds) for this function.
                format: int64
                type: integer
              typicalInputs:
                description: The typical inputs of various sizes.
                items:
                  description: Describes an input for a serverless function.
                  properties:
                    message:
                      description: "The message that will be sent to the function's
                        endpoint to trigger it with the input of the specified size.
                        \n This must be a complete message for the function's endpoint
                        and it must reference the input of the specified size."
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    sizeBytes:
                      description: The size of the input in bytes.
                      format: int64
                      type: integer
                  required:
                  - message
                  - sizeBytes
                  type: object
                type: array
            required:
            - functionContainer
            - functionName
            - maxResponseTimeMs
            - typicalInputs
            type: object
          status:
            description: FunctionDescriptionStatus defines the observed state of FunctionDescription
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
